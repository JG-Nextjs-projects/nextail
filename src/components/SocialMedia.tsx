import React from 'react';

import Linkedin from './icons/linkedin.svg';

const SocialMedia = () => {
  const components = {
    linkedin: Linkedin,
  };

  console.log(components.linkedin);

  return (
    <ul>
      <li></li>
    </ul>
  );
};

export default SocialMedia;
